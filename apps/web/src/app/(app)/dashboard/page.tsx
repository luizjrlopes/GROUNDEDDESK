"use client";import {useEffect,useState} from "react";import {api} from "@/lib/api";import PageHead from "@/components/PageHead";export default function Dashboard(){const [d,setD]=useState<any>(null);useEffect(()=>{api('/dashboard').then(setD)},[]);return <><PageHead eyebrow="Operação de suporte" title="Visão geral" description="SLA, capacidade, conhecimento e qualidade das respostas assistidas sem esconder falhas."/><div className="metrics"><div className="metric"><span>Tickets abertos</span><b>{d?.open_tickets??'—'}</b></div><div className="metric"><span>SLA dentro da meta</span><b>{d?.sla_ok??'—'}%</b></div><div className="metric"><span>Groundedness médio</span><b>{d?.groundedness_avg??'—'}</b></div><div className="metric"><span>Docs indexados</span><b>{d?.indexed_documents??'—'}</b></div></div><div className="card"><h3>Auditoria recente</h3>{d?.recent_audit?.map((a:any,i:number)=><div className="row" key={i}><div><b>{a.action}</b><small>{a.detail}</small></div><span>{a.resource}</span></div>)}</div></>}