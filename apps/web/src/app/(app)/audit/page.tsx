"use client";import {useEffect,useState} from "react";import {api} from "@/lib/api";import PageHead from "@/components/PageHead";export default function Audit(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any[]>('/audit').then(setRows)},[]);return <><PageHead eyebrow="Rastreabilidade" title="Auditoria" description="Ações humanas, regras e etapas de recuperação/IA ficam registradas separadamente."/><div className="card">{rows.map(a=><div className="auditRow" key={a.id}><small>{new Date(a.at).toLocaleString('pt-BR')}</small><span>{a.actor}<br/><small>{a.role}</small></span><div><b>{a.action}</b><small>{a.detail}</small></div><span>{a.resource}</span></div>)}</div></>}