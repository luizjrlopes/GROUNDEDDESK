"use client";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {getSession} from "@/lib/session";export default function AuthGate({children}:{children:React.ReactNode}){const r=useRouter();const [ok,setOk]=useState(false);useEffect(()=>{if(!getSession())r.replace('/');else setOk(true)},[r]);return ok?<>{children}</>:<div className="loading">Carregando…</div>}